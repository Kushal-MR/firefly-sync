"""PHASE 1 — run the virtual swarm.

Owns the loop: step the swarm, measure, draw, repeat. The swarm does not
know this file exists.

Usage (planned):
    python scripts/run_sim.py --drones 20 --coupling 0.1 --range 200
    python scripts/run_sim.py --drones 20 --no-window --seconds 60

--no-window must work. If the simulation cannot run without a display, the
architecture rule in docs/PLAN.md has been broken and Phase 3 is blocked.

TODO(phase-1): implement.
"""
