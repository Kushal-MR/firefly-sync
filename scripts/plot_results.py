"""PHASE 3 — turn data/*.csv into the report figures.

Every figure in the report is generated here. Nothing hand-drawn, nothing
typed in by hand, so any number can be regenerated with one command.

Usage (planned):
    python scripts/plot_results.py --all

Figures (see docs/EXPERIMENTS.md):
    1. time-to-sync vs delay, simple vs Reachback
    2. steady-state phase spread vs packet loss
    3. cycles-to-sync vs swarm size
    4. phase spread over time for a late joiner
    5. simulation vs hardware on the same axes

TODO(phase-3): implement.
"""
